import type { AnyData } from '../types';
/**
 * Defines all provided properties as non-enumerable, configurable, values
 */
export declare const defineValues: <M extends AnyData, D extends AnyData>(data: M, properties: D) => M;
/**
 * Defines all provided properties as non-enumerable, configurable, getters
 */
export declare const defineGetters: <M extends AnyData, D extends AnyData>(data: M, properties: D) => M;
/**
 * Defines all provided properties as non-enumerable, configurable, setters
 */
export declare const defineSetters: <M extends AnyData, D extends AnyData>(data: M, properties: D) => M;
